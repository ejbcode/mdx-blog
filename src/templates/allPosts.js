import React from 'react';
import { graphql } from 'gatsby';
import { Container, Content, ContentCard, FeautureImage, Pagination } from '../components';
import { H1, P } from '../elements';

const allPosts = () => {
  console.log('s');

  return (
    <div>
      <p>hola</p>
    </div>
  );
};

export default allPosts;
